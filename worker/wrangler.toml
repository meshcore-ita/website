name = "meshcore-ita-bot"
main = "worker.mjs"
compatibility_date = "2026-09-01"

# Espone il Worker su <name>.<subdomain>.workers.dev
workers_dev = true

# Chat del gruppo MeshCore ITA: il bot ignora gli update di altre chat.
[vars]
TELEGRAM_CHAT_ID = "-1003711129218"

# Secret (non in questo file):
#   npx wrangler secret put TELEGRAM_BOT_TOKEN
#   npx wrangler secret put TELEGRAM_WEBHOOK_SECRET
